# Home-Assistant-Integrationen, deren Python-Pakete die Tests brauchen.
#
# Nicht die Pakete selbst: die CI liest sie aus Home Assistants eigenen
# Manifesten, samt der Manifeste ihrer Abhaengigkeiten. Eine feste
# Paketliste hier waere beim naechsten Update falsch -- und zwar auf die
# stille Art, weil pip die alte Version klaglos nimmt.
#
# Warum ueberhaupt: der Adapter importiert homeassistant.components.
# bluetooth erst zur Laufzeit und faengt ImportError ab, damit er ohne
# die Integration laeuft. Ein Test, der den Weg MIT Bluetooth prueft,
# muss sie dagegen wirklich importieren koennen.
bluetooth
